# Auto-generated source chunk. Keep small for mobile-friendly file architecture.
SOURCE = '"""2026-06-02 full correctness patch for 10-point reversal engine.\n\nNon-destructive runtime patch. It keeps the original files/functions in place and\nonly overrides the calculation layer after the legacy/split Home module is loaded.\n\n2026-06-03 v2: adds stronger noise cancellation so transition-only setups stay as 6/10 WATCH until shock confirmation appears.\n\nFixes:\n- 10/10 detector now really evaluates 10 independent drivers; DVE is counted when\n  it confirms the rotation, not hidden as a non-counted watch row.\n- Detects capitulation -> reversal setups from context windows, not only selected\n  rows, but now requires confirmation gates so partial/noisy setups do not become\n  false 7/10 alerts.\n- Home "Last Time >= 7/10" scans the loaded dataframe from the current loaded day\n  back 25 days and always returns either the latest exact >=7/10 hour or the best\n  scanned hour with day/hour/score details.\n- Current Home score and Finder selected-hour score use the same evaluator.\n"""\n\nfrom __future__ import annotations\n\n\ndef install(g: dict) -> None:\n    import math\n    import pandas as pd\n    import streamlit as st\n\n    _safe_num = g.get("_safe_num", lambda v, default=0.0: default)\n    _normalize_local = g.get("_normalize_local")\n    _finder_actual_interval_minutes = g.get("_finder_actual_interval_minutes")\n    _finder_market_snapshot = g.get("_finder_market_snapshot")\n    _format_reversal_period_label = g.get("_format_reversal_period_label", lambda ts: str(ts))\n\n    def _num(v, default=0.0):\n        try:\n            x = float(v)\n            if math.isnan(x) or math.isinf(x):\n                return default\n            return x\n        except Exception:\n            try:\n                return float(_safe_num(v, default))\n            except Exception:\n                return default\n\n    def _bool_text(ok):\n        return "YES" if bool(ok) else "NO"\n\n    def _status_from_count(count: int):\n        if count >= 9:\n            return "EXTREME", "EXTREME REVERSAL DANGER", "red"\n        if count >= 7:\n            return "DANGER", "IMPORTANT REVERSAL DANGER", "red"\n        if count >= 5:\n            return "WARNING", "EARLY REVERSAL WARNING", "orange"\n        return "NORMAL", "NORMAL / NO STRONG REVERSAL", "green"\n\n    def _enhanced_reversal_eval(before, after):\n        """10-driver evaluator designed for one-hour capitulation/reversal events."""\n        before = before or {}\n        after = after or {}\n        bm, am = _num(before.get("move_%")), _num(after.get("move_%"))\n        bdve, adve = _num(before.get("dve_%")), _num(after.get("dve_%"))\n        brise, arise = _num(before.get("rising_eff_%")), _num(after.get("rising_eff_%"))\n        bfall, afall = _num(before.get("falling_eff_%")), _num(after.get("falling_eff_%"))\n        bfat, afat = _num(before.get("fat_tail_z")), _num(after.get("fat_tail_z"))\n        bk, ak = _num(before.get("kurtosis")), _num(after.get("kurtosis"))\n        btrust, atrust = _num(before.get("trust_%")), _num(after.get("trust_%"))\n        bbuy, abuy = _num(before.get("buy_%")), _num(after.get("buy_%"))\n        bsell, asell = _num(before.get("sell_%")), _num(after.get("sell_%"))\n\n        dmove = round(am - bm, 5)\n        ddve = round(adve - bdve, 5)\n        drise = round(arise - brise, 5)\n        dfall_drop = round(bfall - afall, 5)\n        dfat = round(afat - bfat, 5)\n        dk = round(ak - bk, 5)\n        dtrust = round(atrust - btrust, 5)\n        dbuy = round(abuy - bbuy, 5)\n        dsell = round(asell - bsell, 5)\n        dsell_weak = round(bsell - asell, 5)\n        dbuy_weak = round(bbuy - abuy, 5)\n        k_ratio = (ak / bk) if abs(bk) > 1e-9 else (999.0 if ak > 0 else 0.0)\n\n        sell_capitulation = bsell >= 58 and (abuy >= 38 or dbuy >= 5) and asell <= max(62, bsell - 3)\n        buy_capitulation = bbuy >= 58 and (asell >= 38 or dsell >= 5) and abuy <= max(62, bbuy - 3)\n        side_recovery = sell_capitulation or buy_capitulation\n        sign_flip = (bm < 0 < am) or (bm > 0 > am)\n\n        # Adaptive thresholds: XAUUSD hourly moves may be < 4% but still huge in pips.\n        direction_rotation = sign_flip and abs(dmove) >= 0.12 or abs(dmove) >= 0.55 or (side_recovery and abs(dmove) >= 0.08)\n        shock_hour = abs(am) >= 0.35 or abs(bm) >= 0.35 or abs(dmove) >= 0.45\n        kurtosis_explosion = ak >= 3.0 or dk >= 1.0 or k_ratio >= 1.6\n        fat_tail_expansion = afat >= 1.25 or dfat >= 0.35 or (shock_hour and afat >= 0.8)\n        side_flip = side_recovery or (abs(dbuy) >= 8 and abs(dsell) >= 8)\n        efficiency_rotation = drise >= 1.0 or dfall_drop >= 1.0 or (side_recovery and (arise >= brise or afall <= bfall))\n        participation_recovery = (sell_capitulation and (dbuy >= 4 or abuy >= 40)) or (buy_capitulation and (dsell >= 4 or asell >= 40))\n        pressure_weakness = (sell_capitulation and (dsell_weak >= 3 or asell <= 60)) or (buy_capitulation and (dbuy_weak >= 3 or abuy <= 60))\n        trust_confirmation = atrust >= 55 or dtrust >= 3 or (shock_hour and atrust >= btrust)\n        dve_rotation = abs(ddve) >= 6 or (adve >= 35 and abs(dmove) >= 0.15) or (side_recovery and adve >= 20)\n\n        # Final score confirms that several weaker but aligned signals become a valid 7/10 warning.\n        weighted = 0.0\n        weights = {\n            "Direction Rotation": 18.0,\n            "Shock / One-Hour Move": 12.0,\n            "Kurtosis Explosion": 12.0,\n            "Fat Tail Expansion": 10.0,\n            "Side Flip / Recovery": 14.0,\n            "Efficiency Rotation": 8.0,\n            "Participation Recovery": 8.0,\n            "Pressure Weakness": 7.0,\n            "Trust Confirmation": 6.0,\n            "DVE Rotation": 5.0,\n        }\n        raw = [\n            ("Direction Rotation", direction_rotation, dmove, "Very High", "move changes direction or rotates strongly"),\n            ("Shock / One-Hour Move", shock_hour, f"before {bm:.3f}% → after {am:.3f}%", "Very High", "large current/selected hour move"),\n            ("Kurtosis Explosion", kurtosis_explosion, f"Δ {dk:.3f} | ratio {k_ratio:.2f}", "High", "tail distribution expands"),\n            ("Fat Tail Expansion", fat_tail_expansion, f"Δ {dfat:.3f} | after {afat:.3f}", "High", "abnormal candle shock appears"),\n            ("Side Flip / Recovery", side_flip, f"BUY {bbuy:.1f}→{abuy:.1f} | SELL {bsell:.1f}→{asell:.1f}", "High", "dominant side loses control"),\n            ("Efficiency Rotation", efficiency_rotation, f"rise Δ {drise:.2f} | fall-drop {dfall_drop:.2f}", "Medium", "movement becomes cleaner after shock"),\n            ("Participation Recovery", participation_recovery, f"BUY Δ {dbuy:.1f} | SELL Δ {dsell:.1f}", "Medium", "opposite side starts participating"),\n            ("Pressure Weakness", pressure_weakness, f"sell weak {dsell_weak:.1f} | buy weak {dbuy_weak:.1f}", "Medium", "old dominant pressure weakens"),\n            ("Trust Confirmation", trust_confirmation, f"{btrust:.1f}→{atrust:.1f}", "Confirmation", "trust improves or holds during reversal"),\n            ("DVE Rotation", dve_rotation, f"Δ {ddve:.2f} | after {adve:.2f}", "Confirmation", "directional efficiency confirms rotation"),\n        ]\n        for name, ok, *_ in raw:\n            if ok:\n                weighted += weights[name]\n\n        raw_active = sum(1 for _, ok, *_ in raw if ok)\n\n        # 2026-06-03 STRICT NOISE-GATED REVERSAL UPGRADE\n        # Goal: catch 09:00-15:00 trend-stop / transition reversal, but do NOT\n        # auto-promote every 6/10-8/10 noisy cluster into a 7/10 danger alert.\n        # Final danger requires: raw drivers + exhaustion + pressure transfer +\n        # shock move, with a low-data/low-structure noise blocker.\n        primary_count = int(bool(direction_rotation)) + int(bool(shock_hour)) + int(bool(side_flip))\n        tail_confirm = bool(kurtosis_explosion or fat_tail_expansion)\n        flow_confirm = bool(efficiency_rotation or participation_recovery or pressure_weakness)\n        model_confirm = bool(trust_confirmation or dve_rotation)\n        opposite_side_confirm = bool(participation_recovery and pressure_weakness)\n\n        before_rows = int(_num(before.get("rows"), 0))\n        after_rows = int(_num(after.get("rows"), 0))\n        enough_context = bool(before_rows >= 8 and after_rows >= 8)\n\n        # Trend exhaustion = old side was dominant, then losing control / efficiency.\n        trend_exhaustion = bool(\n            ((bsell >= 58 and (dsell_weak >= 3 or afall <= bfall or dfall_drop >= 1.0))\n             or (bbuy >= 58 and (dbuy_weak >= 3 or afall <= bfall or dfall_drop >= 1.0))\n             or (tail_confirm and pressure_weakness)\n             or (shock_hour and abs(dmove) >= 0.35 and flow_confirm))\n        )\n\n        # Pressure transfer = opposite side participates AND old pressure weakens.\n        pressure_transfer = bool(\n            (side_flip and opposite_side_confirm)\n            or (sell_capitulation and abuy >= 40 and dsell_weak >= 3)\n            or (buy_capitulation and asell >= 40 and dbuy_weak >= 3)\n        )\n\n        # Shock move = real displacement or abnormal tail, not tiny sideways churn.\n        shock_move = bool(\n            (shock_hour and (tail_confirm or abs(dmove) >= 0.35))\n            or abs(am) >= 0.45\n            or abs(bm) >= 0.45\n        )\n\n        # Low-volume/noise proxy: not enough candles, tiny move, weak trust/DVE,\n        # and no tail/shock structure. This avoids false 7/10 alerts in duplicated\n        # or very sparse Finder windows.\n        low_volume_noise = bool(\n            (not enough_context)\n            or (abs(dmove) < 0.12 and abs(am) < 0.18 and abs(bm) < 0.18 and not tail_confirm)\n            or (atrust < 35 and adve < 15 and not shock_hour and not tail_confirm)\n        )\n\n        # Extra 2026-06-03 v2 noise cancellation.\n        # The detector must separate real reversal structure from repeated/sparse\n        # frame noise and from simple trend continuation.\n        structure_quality_score = int(bool(enough_context)) + int(bool(trend_exhaustion)) + int(bool(pressure_transfer)) + int(bool(shock_move)) + int(bool(tail_confirm)) + int(bool(flow_confirm))\n        micro_chop_noise = bool(abs(dmove) < 0.16 and abs(am) < 0.22 and abs(bm) < 0.22 and raw_active < 9)\n        no_pressure_reversal = bool(raw_active >= 7 and not pressure_transfer)\n        continuation_not_reversal = bool(\n            raw_active >= 7\n            and not sign_flip\n            and not side_recovery\n            and ((bm > 0 and am > 0 and abuy >= bbuy) or (bm < 0 and am < 0 and asell >= bsell))\n        )\n        weak_model_noise = bool(raw_active >= 7 and primary_count < 2 and not tail_confirm)\n        duplicate_or_sparse_noise = bool(not enough_context and not (shock_hour and tail_confirm and pressure_transfer))\n        strict_noise_block = bool(low_volume_noise or micro_chop_noise or no_pressure_reversal or continuation_not_reversal or weak_model_noise or duplicate_or_sparse_noise)\n        transition_warning = bool(trend_exhaustion and pressure_transfer and not shock_move and not strict_noise_block)\n\n        capitulation_pattern = bool(\n            side_recovery\n            and trend_exhaustion\n            and pressure_transfer\n            and (tail_confirm or shock_move)\n        )\n        early_reversal_valid = bool(\n            raw_active >= 7\n            and trend_exhaustion\n            and pressure_transfer\n            and shock_move\n            and not low_volume_noise\n        )\n        confirmed_reversal = bool(\n            early_reversal_valid\n            and primary_count >= 2\n            and flow_confirm\n            and (tail_confirm or model_confirm)\n        )\n        two_strong_opposite_candles_after_peak = bool(\n            pressure_transfer\n            and shock_move\n            and tail_confirm\n            and ((sell_capitulation and abuy >= 45) or (buy_capitulation and asell >= 45))\n            and after_rows >= 2\n        )\n        strong_capitulation_reversal = bool(\n            capitulation_pattern\n            and confirmed_reversal\n            and opposite_side_confirm\n            and raw_active >= 7\n        )\n\n        active = int(raw_active)\n        noise_filter_note = "PASS"\n\n        # Safe scoring ladder requested by user:\n        # - Raw 7/10 but no exhaustion => keep/cap 6/10\n        # - Raw 7/10 + exhaustion only => keep/cap 6/10\n        # - Raw 7/10 + exhaustion + pressure transfer => 7/10 only when noise is clean\n        # - Raw 8/10 + exhaustion + pressure transfer + shock => 8/10\n        # - Two strong opposite candles after peak => 8-9/10\n        if raw_active >= 7:\n            if low_volume_noise:\n                active = min(active, 6)\n                weighted = min(weighted, 64.0)\n                noise_filter_note = "BLOCKED: low-context / low-structure noise"\n            elif not trend_exhaustion:\n                active = min(active, 6)\n                weighted = min(weighted, 64.0)\n                noise_filter_note = "BLOCKED: raw 7/10 without trend exhaustion"\n            elif not pressure_transfer:\n                active = min(active, 6)\n                weighted = min(weighted, 64.0)\n                noise_filter_note = "BLOCKED: exhaustion only, no pressure transfer"\n            elif transition_warning:\n                # Important: this is a valid early warning, but not full 7/10 danger\n                # until shock/fat-tail/opposite-side confirmation appears.\n                active = min(active, 6)\n                weighted = max(min(weighted, 64.0), 60.0)\n                noise_filter_note = "WATCH: transition warning only, no shock confirmation"\n            elif early_reversal_valid:\n                active = max(active, 7)\n                weighted = max(weighted, 72.0)\n                noise_filter_note = "PASS: strict exhaustion + pressure transfer + shock gate"\n                if raw_active >= 8:\n                    active = max(active, 8)\n                    weighted = max(weighted, 82.0)\n                    noise_filter_note = "PASS: raw 8/10 with strict shock confirmation"\n                if two_strong_opposite_candles_after_peak:\n                    active = max(active, 9 if raw_active >= 9 else 8)\n                    weighted = max(weighted, 88.0)\n                    noise_filter_note = "PASS: two strong opposite-side candles after peak"\n\n        # 2026-06-03 V3 REVERSAL QUALITY GATE\n        # Purpose: improve quality, correctness, and efficiency without deleting the\n        # original 10-driver engine. V3 is a second-pass validator. It allows a\n        # full 7/10+ only when the signal has enough independent structure:\n        # context + exhaustion + pressure transfer + shock/tail + flow/model.\n        v3_core_confirm_count = (\n            int(bool(enough_context))\n            + int(bool(trend_exhaustion))\n            + int(bool(pressure_transfer))\n            + int(bool(shock_move))\n            + int(bool(tail_confirm))\n            + int(bool(flow_confirm))\n            + int(bool(model_confirm))\n        )\n        v3_quality_score = 0.0\n        v3_quality_score += 14.0 if enough_context else 0.0\n        v3_quality_score += 18.0 if trend_exhaustion else 0.0\n        v3_quality_score += 22.0 if pressure_transfer else 0.0\n        v3_quality_score += 18.0 if shock_move else 0.0\n        v3_quality_score += 10.0 if tail_confirm else 0.0\n        v3_quality_score += 10.0 if flow_confirm else 0.0\n        v3_quality_score += 8.0 if model_confirm else 0.0\n        v3_quality_score -= 18.0 if micro_chop_noise else 0.0\n        v3_quality_score -= 16.0 if continuation_not_reversal else 0.0\n        v3_quality_score -= 16.0 if duplicate_or_sparse_noise else 0.0\n        v3_quality_score -= 14.0 if no_pressure_reversal else 0.0\n        v3_quality_score = round(max(0.0, min(100.0, v3_quality_score)), 2)\n\n        v3_block_reasons = []\n        if not enough_context:\n            v3_block_reasons.append("not enough before/after context")\n        if not trend_exhaustion:\n            v3_block_reasons.append("no clear old-trend exhaustion")\n        if not pressure_transfer:\n            v3_block_reasons.append("no pressure transfer to opposite side")\n        if not shock_move:\n            v3_block_reasons.append("no shock/fat-tail displacement")\n        if micro_chop_noise:\n            v3_block_reasons.append("micro-chop / tiny movement")\n        if continuation_not_reversal:\n            v3_block_reasons.append("looks like continuation, not reversal")\n        if duplicate_or_sparse_noise:\n            v3_block_reasons.append("duplicate/sparse Finder structure")\n        if weak_model_noise:\n            v3_block_reasons.append("weak model confirmation")\n\n        v3_pass_full_reversal = bool(\n            raw_active >= 7\n            and v3_quality_score >= 70\n            and v3_core_confirm_count >= 5\n            and trend_exhaustion\n            and pressure_transfer\n            and shock_move\n            and not strict_noise_block\n        )\n        v3_transition_only = bool(\n            raw_active >= 6\n            and v3_quality_score >= 50\n            and trend_exhaustion\n            and (pressure_transfer or flow_confirm)\n            and not v3_pass_full_reversal\n        )\n\n        # Final blocker: no danger-level score without the strict V2 + V3 quality gate.\n        if active >= 7 and strict_noise_block:\n            active = 6\n            weighted = min(weighted, 64.0)\n            noise_filter_note = "BLOCKED: v2 noise filter rejected weak/duplicate/continuation structure"\n        elif active >= 7 and not early_reversal_valid:\n            active = 6\n            weighted = min(weighted, 64.0)\n            noise_filter_note = "BLOCKED: 7/10 requires exhaustion + pressure transfer + shock"\n        elif active >= 7 and not v3_pass_full_reversal:\n            active = 6\n            weighted = min(weighted, 64.0)\n            noise_filter_note = "BLOCKED: V3 quality gate failed - " + (", ".join(v3_block_reasons[:3]) if v3_block_reasons else "insufficient independent confirmation")\n        elif active >= 7 and weighted < 65:\n            active = 6\n            noise_filter_note = "BLOCKED: weighted evidence below 65"\n        elif active < 7 and v3_transition_only:\n            active = max(active, 6)\n            weighted = max(weighted, 60.0)\n            noise_filter_note = "WATCH: V3 transition warning; wait for shock confirmation before full 7/10"\n        elif active < 7 and noise_filter_note == "PASS":\n            noise_filter_note = "WATCH: partial evidence only"\n\n        if active >= 7 and v3_pass_full_reversal:\n            weighted = max(weighted, 72.0)\n            noise_filter_note = "PASS: V3 high-quality reversal confirmed"\n\n        weighted = round(max(0.0, min(100.0, weighted)), 2)\n        probability = int(round(active * 10))\n        status, title, color = _status_from_count(active)\n        rows = []\n        thresholds = {\n            "Direction Rotation": "sign flip + 0.12% move, OR >=0.55% rotation, OR capitulation rotation",\n            "Shock / One-Hour Move": "abs current/before move >=0.35% OR delta >=0.45%",\n            "Kurtosis Explosion": "after >=3.0 OR delta >=1.0 OR ratio >=1.6x",\n            "Fat Tail Expansion": "after z >=1.25 OR delta >=0.35",\n            "Side Flip / Recovery": "dominant BUY/SELL >=58% then opposite side recovers >=38% or +5%",\n            "Efficiency Rotation": "rising efficiency +1 OR falling efficiency drops +1",\n            "Participation Recovery": "opposite candle participation recovers +4% or reaches 40%",\n            "Pressure Weakness": "old side weakens by >=3% or no longer expands",\n            "Trust Confirmation": "after trust >=55 OR trust delta >=3",\n            "DVE Rotation": "DVE delta >=6 OR after DVE >=35 with rotation",\n        }\n        icons = ["🔵", "⚡", "🔴", "🔴", "🔵", "🔵", "🟢", "🟡", "🟢", "🟣"]\n        for i, (name, ok, value, impact, note) in enumerate(raw, 1):\n            rows.append({\n                "rank": i,\n                "driver": name,\n                "triggered": _bool_text(ok),\n                "light": icons[i-1] if ok else "⚪",\n                "value_or_change": value,\n                "impact": impact,\n                "threshold": thresholds[name],\n                "note": note,\n            })\n        return {\n            "active_count": int(active),\n            "probability_pct": int(probability),\n            "status": status,\n            "title": title,\n            "color": color,\n            "weighted_score": weighted,\n            "raw_active_count": int(raw_active),\n            "confirmed_reversal": bool(confirmed_reversal),\n            "noise_filter": noise_filter_note,\n            "primary_confirm_count": int(primary_count),\n            "tail_confirm": bool(tail_confirm),\n            "flow_confirm": bool(flow_confirm),\n            "model_confirm": bool(model_confirm),\n            "capitulation_pattern": bool(capitulation_pattern),\n            "strong_capitulation_reversal": bool(strong_capitulation_reversal),\n            "early_reversal_valid": bool(early_reversal_valid),\n            "trend_exhaustion": bool(trend_exhaustion),\n            "pressure_transfer": bool(pressure_transfer),\n            "shock_move": bool(shock_move),\n            "low_volume_noise": bool(low_volume_noise),\n            "two_strong_opposite_candles_after_peak": bool(two_strong_opposite_candles_after_peak),\n            "structure_quality_score": int(structure_quality_score),\n            "quality_score_v3": float(v3_quality_score),\n            "quality_gate_v3": "PASS" if bool(v3_pass_full_reversal) else ("WATCH" if bool(v3_transition_only) else "BLOCK"),\n            "v3_core_confirm_count": int(v3_core_confirm_count),\n            "v3_block_reasons": v3_block_reasons,\n            "v3_required_confirmations": "Need context + exhaustion + pressure transfer + shock/tail + flow/model before 7/10 danger",\n            "micro_chop_noise": bool(micro_chop_noise),\n            "no_pressure_reversal": bool(no_pressure_reversal),\n            "continuation_not_reversal": bool(continuation_not_reversal),\n            "weak_model_noise": bool(weak_model_noise),\n'
